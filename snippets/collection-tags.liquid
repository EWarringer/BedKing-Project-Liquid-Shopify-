{% if collection.all_tags.size > 0 %}
  <div class="form-horizontal">
    <label for="sortTags">{{ 'collections.tags.title' | t }}</label>
    <select name="sortTags" id="sortTags">
      {% if collection.handle %}
        <option value="/collections/{{ collection.handle }}">{{ 'collections.tags.all_of_collection' | t }}</option>
      {% elsif collection.current_type %}
        <option value="{{ collection.current_type | url_for_type }}">{{ 'collections.tags.all_of_collection' | t }}</option>
      {% elsif collection.current_vendor %}
        <option value="{{ collection.current_vendor | url_for_vendor }}">{{ 'collections.tags.all_of_collection' | t }}</option>
      {% endif %}
      {% for tag in collection.all_tags %}
        <option value="/collections/{% if collection.handle != blank %}{{ collection.handle }}{% else %}all{% endif %}/{{ tag | handleize }}"{% if current_tags contains tag %} selected="selected"{% endif %}>{{ tag }}</option>
      {% endfor %}
    </select>
  </div>
{% endif %}
